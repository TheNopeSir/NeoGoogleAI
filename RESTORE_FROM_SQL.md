# 🔄 Восстановление изображений из SQL дампа

## Шаг 1: Подготовка SQL файла

Загрузите ваш SQL дамп в проект:

```bash
# Способ 1: Через SCP
scp /path/to/backup.sql user@server:/home/user/NeoGoogleAI/

# Способ 2: Создать файл вручную
nano backup.sql
# Вставьте содержимое SQL дампа
# Ctrl+X, Y, Enter для сохранения

# Способ 3: Скачать с удаленного сервера
wget https://your-server.com/path/to/backup.sql
# или
curl -O https://your-server.com/path/to/backup.sql
```

## Шаг 2: Создать JSON бэкап из текущей БД

**ВАЖНО:** Сначала создайте JSON бэкап текущего состояния БД на случай, если что-то пойдет не так:

```bash
# Создаем бэкап текущего состояния
node backupDatabase.js
# Это создаст файл: backup_exhibits_<timestamp>.json
```

## Шаг 3: Восстановление из SQL дампа

### Вариант A: Восстановление полной таблицы exhibits

```bash
# 1. Подключитесь к БД и восстановите из дампа
PGPASSWORD='9H@DDCb.gQm.S}' psql -h 185.152.92.64 -U gen_user -d default_db < backup.sql

# 2. Или только таблицу exhibits
PGPASSWORD='9H@DDCb.gQm.S}' psql -h 185.152.92.64 -U gen_user -d default_db << 'EOF'
-- Если нужно, очистите таблицу
TRUNCATE TABLE exhibits;
-- Затем восстановите
\i backup.sql
EOF
```

### Вариант B: Через специальный скрипт (безопаснее)

```bash
# Используйте скрипт для импорта
node importFromSQL.js backup.sql

# Он автоматически:
# 1. Создаст резервную копию текущей БД
# 2. Восстановит данные из SQL
# 3. Покажет статистику
```

## Шаг 4: Создать JSON бэкап из восстановленной БД

```bash
# После восстановления SQL создайте JSON бэкап
node backupDatabase.js

# Это создаст файл: backup_exhibits_<timestamp>.json
# В нем будут все артефакты с изображениями из SQL дампа
```

## Шаг 5: Умное восстановление изображений base64 → WebP

**НОВОЕ!** Теперь используется умное восстановление, которое:
- ✅ Проверяет существование WebP файлов на диске
- ✅ Переиспользует существующие файлы (быстрее!)
- ✅ Создает новые только если необходимо
- ✅ Предотвращает потерю изображений при повторном восстановлении

### Способ 1: Умное восстановление (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Проверьте статистику
node smartRestoreImages.js

# 2. Запустите умное восстановление
node smartRestoreImages.js --execute

# 3. Для больших баз данных - с лимитом
node smartRestoreImages.js --execute --limit=100
```

### Способ 2: Обычная миграция (старый метод)

```bash
# 1. Проверьте что в БД есть изображения
node diagnoseImages.js

# 2. Запустите миграцию
node migrateImages.js --execute

# Это создаст оптимизированные WebP файлы для всех base64 изображений
```

**Разница между методами:**
- **Умное восстановление**: Проверяет диск, переиспользует файлы → быстрее, экономит место
- **Обычная миграция**: Всегда создает новые файлы → медленнее, может дублировать

## Шаг 6: Проверка результатов

```bash
# Проверьте результаты миграции
node diagnoseImages.js

# Должно быть:
# ✅ Обработанные (WebP): XX артефактов
# ❌ Битые пути (404): 0
```

## 🌐 Альтернатива: Через веб-интерфейс (Админ-панель)

Теперь можно восстанавливать изображения через админ-панель:

1. Откройте админ-панель в приложении
2. Перейдите в раздел "Управление изображениями"
3. Нажмите "Умное восстановление изображений"
4. Дождитесь завершения процесса

**API endpoint:** `POST /api/admin/smart-restore-images`

```bash
# Или через curl:
curl -X POST http://localhost:3001/api/admin/smart-restore-images
```

## 🔧 Альтернативный подход: Прямое восстановление артефактов

Если SQL дамп содержит только данные (INSERT statements):

```bash
# 1. Извлечь данные артефактов в JSON
node extractExhibitsFromSQL.js backup.sql > extracted_backup.json

# 2. Восстановить из JSON
node restoreFromBackup.js extracted_backup.json --execute

# 3. Умное восстановление изображений (РЕКОМЕНДУЕТСЯ)
node smartRestoreImages.js --execute

# ИЛИ обычная миграция
node migrateImages.js --execute
```

## 📊 Ожидаемый результат

После успешного восстановления:
- ✅ Все артефакты с изображениями восстановлены
- ✅ Base64 изображения конвертированы в WebP
- ✅ Созданы 4 размера: thumbnail (240x240), medium (800x800), large (1200x1200), placeholder (20x20)
- ✅ Прогрессивная загрузка работает

## ⚠️ Важные замечания

1. **Резервная копия**: Всегда создавайте JSON бэкап ПЕРЕД импортом SQL
2. **Подключение**: Убедитесь что БД доступна (проверьте через `node diagnoseImages.js`)
3. **Размер**: Миграция больших объемов изображений может занять время
4. **Место на диске**: Убедитесь что есть достаточно места в `uploads/images/`

## 🆘 Troubleshooting

### Ошибка подключения к БД
```bash
# Проверьте настройки в .env
cat .env

# Проверьте доступность БД
PGPASSWORD='9H@DDCb.gQm.S}' psql -h 185.152.92.64 -U gen_user -d default_db -c "SELECT COUNT(*) FROM exhibits;"
```

### SQL дамп не загружается
```bash
# Проверьте формат файла
head -20 backup.sql

# Проверьте размер
ls -lh backup.sql
```

### После восстановления нет изображений
```bash
# Проверьте что в SQL дампе есть imageUrls
grep -i "imageUrls" backup.sql | head -5

# Или проверьте через БД
PGPASSWORD='9H@DDCb.gQm.S}' psql -h 185.152.92.64 -U gen_user -d default_db -c "SELECT data->'imageUrls' FROM exhibits LIMIT 1;"

# Если изображения в base64, запустите умное восстановление
node smartRestoreImages.js --execute
```

### Изображения пропадают после повторного восстановления

**Причина:** SQL дамп содержит base64 изображения, которые перезаписывают WebP пути в БД.

**Решение:** Используйте умное восстановление вместо обычной миграции.

```bash
# ❌ Плохо: Обычная миграция создаст дубликаты файлов
node migrateImages.js --execute

# ✅ Хорошо: Умное восстановление переиспользует существующие файлы
node smartRestoreImages.js --execute
```

**Как это работает:**
1. Умное восстановление вычисляет хеш изображения из base64
2. Проверяет, существуют ли WebP файлы с таким хешем
3. Если да - использует существующие пути (мгновенно!)
4. Если нет - создает новые файлы
5. Обновляет БД с правильными путями
