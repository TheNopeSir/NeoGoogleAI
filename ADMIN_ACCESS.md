# Доступ администратора

## Администраторы

Пользователи с правами администратора имеют доступ к дополнительным функциям, включая страницу миграции данных.

### Список администраторов по email:
- `kennyornope@gmail.com` (пользователь Truester)

### Как работает проверка администратора

Пользователь считается администратором, если выполняется одно из условий:
1. У пользователя установлен флаг `isAdmin: true` в профиле
2. Email пользователя находится в списке `ADMIN_EMAILS` (см. `App.tsx`)

## Доступ к странице миграции

### Для пользователя с email kennyornope@gmail.com:

1. **Войдите в систему** используя email `kennyornope@gmail.com` и ваш пароль

2. **Перейдите в раздел "СОЗДАТЬ"** (CREATE_HUB)
   - Нажмите на кнопку "СОЗДАТЬ" в главном меню

3. **Увидите кнопку "МИГРАЦИЯ ДАННЫХ"** (оранжевая, внизу списка)
   - Это последняя кнопка после "НОВЫЙ АРТЕФАКТ", "НОВАЯ КОЛЛЕКЦИЯ" и "В ПОИСКЕ"

4. **Нажмите "МИГРАЦИЯ ДАННЫХ"**
   - Откроется страница с инструментами миграции

5. **На странице миграции доступны:**
   - 🔄 **Миграция реакций** - конвертация всех реакций в LIKE
   - 📖 **Проверка гостевой книги** - просмотр статистики гостевой книги

## Добавление новых администраторов

Чтобы добавить нового администратора по email, отредактируйте файл `App.tsx`:

```typescript
// Admin users
const ADMIN_EMAILS = [
    'kennyornope@gmail.com',
    'newemail@example.com'  // Добавьте новый email здесь
];
```

Либо установите флаг `isAdmin: true` в профиле пользователя в базе данных:

```sql
UPDATE users
SET data = jsonb_set(data, '{isAdmin}', 'true')
WHERE username = 'username_here';
```

## Безопасность

- ⚠️ Доступ к странице миграции имеют только администраторы
- ⚠️ Миграция данных - необратимая операция, будьте осторожны
- ⚠️ Храните список администраторов в секрете
- ⚠️ Используйте сильные пароли для учетных записей администраторов

## Функции администратора

### Текущие возможности:
- ✅ Доступ к странице миграции данных
- ✅ Запуск миграции реакций
- ✅ Проверка состояния гостевой книги
- ✅ Удаление любых комментариев (в ExhibitDetailPage)

### Планируемые возможности:
- Управление пользователями
- Модерация контента
- Просмотр системной статистики
- Резервное копирование данных
